import 'package:flutter/material.dart';

class terdekat extends StatelessWidget {
  @override
    Widget build(BuildContext context) {
      return Container(
        child: Center(
          child: Text('TERDEKAAATTTTT'),
        ),
      );
    }
}